import { createStyles } from "@material-ui/core";

export const styles = () =>
  createStyles({
    root: {
      height: "100vh",
    },
    navbar: {
      height: "8%",
    },
    view: {
      height: "92%",
    },
  });
