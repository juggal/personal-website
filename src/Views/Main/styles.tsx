import { createStyles, Theme } from "@material-ui/core";

export const styles = ({}: Theme) =>
  createStyles({
    root: {
      height: "100vh",
    },
    navbar: {
      height: "8%",
    },
    view: {
      height: "92%",
    },
  });
